server:
  port: 8080

spring:
  application:
    name: pg-manager-backend
  datasource:
    url: jdbc:mysql://localhost:3306/pg_manager?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: false

app:
  # Base URL the tenant self check-in QR points to. Must be reachable from the
  # tenant's phone (a LAN IP for local testing, or your public domain in prod).
  public-base-url: ${APP_PUBLIC_BASE_URL:http://192.168.1.25:8080}
  security:
    jwt-secret: change-this-development-secret-change-before-production-123456789
    access-token-minutes: 30
    refresh-token-days: 14

springdoc:
  swagger-ui:
    path: /swagger-ui.html
